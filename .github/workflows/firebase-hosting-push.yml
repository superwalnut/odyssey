# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploying UAT
'on':
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: uat
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: npm install
      - run: npm install -g @angular/cli
      - run: ng build --configuration uat --output-path=public
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_HBC666_UAT }}'
          channelId: live
          projectId: hbc-uat-9f2ad
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          FIREBASE_API_KEY: '${{ secrets.FIREBASE_UAT_API_KEY }}'
          PRODUCTION: '${{ secrets.PRODUCTION }}'
          MAIL_URL: '${{ secrets.MAIL_URL }}'
          DOMAIN: '${{ secrets.DOMAIN }}'
          API_KEY: '${{ secrets.API_KEY }}'
          AUTH_DOMAIN: '${{ secrets.AUTH_DOMAIN }}'
          PROJECT_ID: '${{ secrets.PROJECT_ID }}'
          STORAGE_BUCKET: '${{ secrets.STORAGE_BUCKET }}'
          MESSAGING_SENDER_ID: '${{ secrets.MESSAGING_SENDER_ID }}'
          APP_ID: '${{ secrets.APP_ID }}'
          MEASUREMENT_ID: '${{ secrets.MEASUREMENT_ID }}'